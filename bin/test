#!/usr/local/bin/zsh
Dest=${1:-OneDrive}
ls -d */* > InFile
cd /ncBackup
while read line
do
	echo $line
	rclone --progress --multi-thread-streams 6 --buffer-size 2G copyto ${line} ${Dest}:ncBackup/${line}
done < InFile
rm InFile
