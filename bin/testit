#!/usr/local/bin/zsh
type emulate >/dev/null 2>/dev/null || alias emulate=true
emulate -L ksh
cd dotfiles
for i in z*; do
	if $(/usr/bin/diff -q ../.$i $i); then
		echo "$i - no"
	else
		echo "$i - yes"
	fi
done
cd ~
